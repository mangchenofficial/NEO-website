标题: NeoUI：手机上的伪系统
日期: 2026年7月12日
分类: 项目介绍
标签: NeoUI, WebOS, 手机伪系统, 前端, JavaScript

NeoUI 是一个运行在浏览器里的手机风格伪系统。它用 HTML、CSS 和 JavaScript 模拟了锁屏、桌面、状态栏、应用启动等一整套手机交互。这个项目最初是为了在前端层面探索操作系统 UI 的设计，后来也成为了我与喜灰之最的喜 PCOS、MXOS 联动的载体之一。

## 一、NeoUI 是什么

打开 NeoUI 的网页，你会看到一个类似手机的界面：

- 锁屏界面，向上轻扫解锁
- 顶部状态栏显示时间、信号、WiFi、电量
- 桌面网格摆放应用图标
- 点击图标启动应用
- 有设置等基础应用

整个项目不需要后端，纯前端运行，可以部署在任何静态托管平台上。

## 二、技术实现

### 1. 纯前端架构

NeoUI 没有使用 React、Vue 之类的前端框架，全部用原生 JavaScript 实现。这样代码量少、加载快、可控性高。

### 2. 手机外壳模拟

页面用一个 `.phone` 容器模拟手机外形，内部 `.screen` 是屏幕区域。所有交互都在这个屏幕区域内完成。

### 3. 锁屏与主屏

- 锁屏显示时间日期和「向上轻扫解锁」提示
- 上滑后切换到主屏
- 主屏显示壁纸、状态栏和应用图标

### 4. 图标与字体

NeoUI 使用了 Google 的 Material Symbols 字体作为系统图标，保持矢量清晰和统一的视觉风格。

### 5. Android 封装

项目里还有一个 `APP/Android` 目录，把前端页面打包进 Android 应用的 assets 中，这意味着 NeoUI 也可以作为一个 Android 应用运行。

## 三、与喜灰之最的喜 PCOS、MXOS 的联动

NeoUI 和 MXOS 都是我与喜灰之最的喜 PCOS 联动项目的一部分。MXOS 更偏向桌面端伪系统，而 NeoUI 专注于手机端体验。两者共享一些设计思路，比如：

- 都在浏览器里模拟完整系统
- 都使用 Material Design 风格的视觉语言
- 都支持应用启动和窗口/全屏切换

后续可能会进一步打通两个项目的内容或应用生态。

## 四、当前状态

目前 NeoUI 最新版本目录是 `26.7.12`，包含：

- 锁屏、主屏、应用启动框架
- 状态栏
- 设置应用
- Android 封装

它仍然是一个前端 Demo，不能真正打电话、发短信或访问手机硬件，但在视觉和交互上已经能还原一部分手机系统的感觉。

## 五、未来方向

- 增加更多内置应用（计算器、天气、相册等）
- 优化触控动画和过渡效果
- 引入应用安装机制
- 与 MXOS 共享应用格式

## 六、体验方式

NeoUI 是 NEO 项目的一部分，源码位于 `F:\NEO UI` 目录。由于它是纯前端项目，直接用浏览器打开 `index.html` 或在本地启动任意静态服务器即可预览。
