标题: NEO OS：一个初中生的自研操作系统
日期: 2026年7月15日
分类: 项目介绍
标签: NEO OS, 自研系统, 操作系统, 初中生

NEO OS 是我从零开始开发的 32 位 x86 操作系统，使用 C 语言和汇编编写。目前它能在 QEMU 模拟器中启动运行，具备图形桌面、窗口管理、命令行、文件系统和基础多任务能力。这篇文章介绍它的现状、已实现的功能和下一步演进方向。

## 一、项目起点

NEO OS 不是一个基于 Linux 的发行版，而是从引导扇区开始一行一行写的独立系统。启动流程完全自己实现：

- 16 位实模式引导加载程序
- 启用 A20 地址线、设置 GDT
- 切换到 32 位保护模式
- 加载内核到 1MB 处并执行

这个从零开始的过程让我真正理解了 CPU 是怎么从通电一路跑到图形桌面的。

## 二、已实现的功能

### 核心系统

- 32 位单体内核（C + 汇编）
- 物理页分配器（4KB 页面）
- 内核堆分配器（kmalloc/kfree）
- 中断描述符表 IDT 与 PIC 配置
- 基础系统调用接口

### 设备驱动

- PS/2 键盘与鼠标驱动
- VGA 图形驱动（320x200 256 色模式）
- 文本模式显示驱动

### 文件系统

- FAT12 完整实现
- 虚拟文件系统 VFS 层
- 块设备接口

### 图形界面

- 图形化桌面环境
- 窗口管理器（最多 16 窗口、拖动、Z 轴、关闭按钮）
- GShell 图形终端

### 多任务

- 协作式多任务调度
- 最多 8 个任务
- ELF 可执行文件加载

### 命令行

系统内置以下 shell 命令：

| 命令 | 功能 |
|------|------|
| help | 显示可用命令 |
| clear | 清屏 |
| echo | 打印文本 |
| about | 显示系统信息 |
| meminfo | 显示内存统计 |
| time | 显示系统时钟计数 |
| ls | 列出目录文件 |
| cat | 显示文件内容 |

## 三、技术架构

```
┌─────────────────────────────────────────┐
│              用户应用程序                │
├─────────────────────────────────────────┤
│  图形终端  │   桌面   │    窗口管理器    │
├─────────────────────────────────────────┤
│              系统调用接口                │
├─────────────────────────────────────────┤
│   任务   │  VFS  │  FAT12 │  ELF加载器  │
├─────────────────────────────────────────┤
│   键盘   │  鼠标  │  视频  │   屏幕显示   │
├─────────────────────────────────────────┤
│  内存管理 │  堆   │ IDT │ PIC │  IRQ    │
├─────────────────────────────────────────┤
│                内核核心                  │
├─────────────────────────────────────────┤
│            引导加载程序 (ASM)            │
└─────────────────────────────────────────┘
```

## 四、下一步演进

NEO OS 还在持续开发中，当前的主要目标是让它从一个「能跑 GUI 的 demo」成长为真正的操作系统：

- 进程与抢占式调度
- FAT32 文件系统读写
- 完整的系统调用接口（int 0x80）
- 用户态与 ring3 保护
- TCP/IP 网络协议栈
- 高分辨率显示与真实显卡驱动
- 浏览器、多媒体播放器
- NEO 应用包（.neap）与应用商店
- 脱离 QEMU，支持真实 PC 启动

这些目标已经写进了 NEO OS 的演进规范，会逐步实现。

## 五、如何体验

项目源码托管在 GitHub，编译需要：

- NASM 汇编器
- i686-elf GCC 交叉编译器
- QEMU 模拟器

```bash
bash build.sh
bash run.sh
```

Windows 下也提供了 `build.ps1` 和 `run.bat`。

## 六、写在最后

NEO OS 对我来说不只是代码，而是理解计算机底层原理的实验场。从引导扇区到图形窗口，每实现一个功能都会对操作系统有更深一层的认识。如果你也在写自己的 OS，欢迎一起交流。
